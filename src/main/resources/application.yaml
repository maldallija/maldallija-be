spring:
  application:
    name: maldallija-be
  profiles:
    active: local

management:
  endpoints:
    web:
      base-path: /api
      path-mapping:
        health: health-check
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
